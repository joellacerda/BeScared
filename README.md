# BeScared! 👻

Um aplicativo iOS para descobrir, favoritar e gerenciar filmes de terror.
Projeto Full Stack utilizando SwiftUI e Java Spring Boot.

## Estrutura

* **ios/**: Aplicativo nativo desenvolvido em SwiftUI.
* **backend/**: API RESTful desenvolvida em Java 21 com Spring Boot e H2 Database.

## Como Rodar

### Backend (API)
1.  Entre na pasta `backend`.
2.  Abra no IntelliJ ou rode `mvn spring-boot:run`.
3.  A API estará disponível em `http://localhost:8080`.

### iOS (App)
1.  Entre na pasta `ios`.
2.  Abra o `BeScared.xcodeproj` no Xcode.
3.  Aguarde o download dos pacotes e rode no Simulador (Cmd + R).

---
*Desenvolvido como projeto de estudo.*
